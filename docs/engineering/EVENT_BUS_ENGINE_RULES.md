Every engine communicates through the Event Bus.

No engine calls another engine directly.

Events are immutable.

Every event is versioned.

Events are persisted.

Failed events enter the Dead Letter Queue.

Retries are automatic.

Events are observable.

Events support horizontal scaling.

The Event Bus is the backbone of MOJAD GOOS.
