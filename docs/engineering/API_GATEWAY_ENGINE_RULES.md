Every request enters through the API Gateway.

No service is exposed directly.

Gateway validates every request.

Authentication is verified before routing.

Authorization is checked before execution.

Gateway collects metrics.

Gateway performs logging.

Gateway supports versioning.

Gateway supports horizontal scaling.

Gateway remains Pi Native.
