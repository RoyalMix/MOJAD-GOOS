AI Router never performs business logic.

AI Router only orchestrates.

Every AI request passes through AI Router.

AI Router uses Knowledge Graph.

AI Router uses Search Engine.

AI Router selects the best AI model.

AI Router remembers conversation context.

AI Router supports multiple AI agents.

AI Router is modular.

AI Router remains Pi Native.
