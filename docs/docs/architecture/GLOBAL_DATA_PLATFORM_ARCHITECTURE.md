# MOJAD Data Platform Rules

Every entity has one global identifier.

Every dataset has an owner.

Every data change is traceable.

Data quality is continuously monitored.

Metadata is mandatory.

Data lineage is preserved.

Synchronization is consistent.

No duplicated source of truth.

Every Engine consumes standardized data.
