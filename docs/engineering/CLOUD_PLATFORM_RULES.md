# MOJAD Cloud Platform Rules

Every service is containerized.

Every deployment supports multiple regions.

Every service has automatic failover.

Every deployment supports horizontal scaling.

Traffic is distributed intelligently.

Secrets are encrypted.

Edge nodes reduce latency.

Deployments are zero downtime.

Infrastructure is Infrastructure-as-Code.

Platform is cloud agnostic.
